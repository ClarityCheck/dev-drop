# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Supabase root CA: only an input to `wrangler cert upload`, never read at
# runtime. Kept out of git — after the upload the certificate lives in
# Cloudflare, and this file can be deleted.
supabase-ca.pem
